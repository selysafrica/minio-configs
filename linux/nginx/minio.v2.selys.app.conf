server {
    listen 80;
    server_name minio.codihub.ci;

    # Redirection vers HTTPS si SSL est configuré
    # Décommentez après avoir obtenu le certificat
    # return 301 https://$server_name$request_uri;

    # Websocket support pour la console MinIO
    location / {
        proxy_pass http://localhost:9012;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-NginX-Proxy true;

        proxy_connect_timeout 300;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        chunked_transfer_encoding off;
    }
}

# HTTPS (décommentez après certbot)
# server {
#     listen 443 ssl http2;
#     server_name minio.codihub.ci;
#
#     ssl_certificate /etc/letsencrypt/live/minio.codihub.ci/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/minio.codihub.ci/privkey.pem;
#
#     location / {
#         proxy_pass http://localhost:9012;
#         proxy_set_header Host $http_host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-NginX-Proxy true;
#
#         proxy_connect_timeout 300;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         chunked_transfer_encoding off;
#     }
# }
